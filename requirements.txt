# web
Flask==2.3.3
gunicorn==21.2.0

# core stack (Python 3.11-compatible wheels)
torch==2.3.1
torchvision==0.18.1

# ultralytics build that supports YOLOv9 blocks like C3k2
ultralytics==8.2.103

# vision/ocr
opencv-python-headless==4.9.0.80
easyocr==1.7.1

# mongo + env
pymongo[srv]==4.7.2
python-dotenv==1.0.1

# pin numpy < 2 to avoid the “module compiled for 1.x” crash paths
numpy<2